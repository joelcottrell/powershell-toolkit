# Operational Notes

Practical detail that belongs somewhere but would bloat the README.

---

## Prerequisites

What must be true before the script is run: modules installed, permissions
granted, network paths reachable, connections established.

## Running it

Where it is normally run from, under what account, and how often.

## Logging

Where logs are written, how they are named, and how long they are kept. Log
directories are ignored by git; only the `.gitkeep` placeholder is tracked.

## Failure modes

Known failure conditions and what each one means:

| Symptom | Likely cause | Resolution |
| --- | --- | --- |
| | | |

## Rollback

How to undo what this script did, if it can be undone. If it cannot, say so
plainly here.

## Performance

Expected runtime at typical scale, and where it degrades.
